export const add = (a: number, b: number) => {
  // this is a comment
  debugger;
  return a + b;
};

export const subtract = (a: number, b: number) => {
  // this is also a  comment
  return a - b;
};
